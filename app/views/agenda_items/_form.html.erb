<%= form_for(agenda_item) do |f| %>
  <% if agenda_item.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(agenda_item.errors.count, "error") %> prohibited this agenda_item from being saved:</h2>
    <ul>
    <% agenda_item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 3 %>
  </div>
  <div class="actions">
    <%= f.hidden_field :meeting_id %>
    <%= f.submit %>
  </div>
<% end %>
